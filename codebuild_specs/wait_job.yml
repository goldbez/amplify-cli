version: 0.2
env:
  shell: bash
phases:
  build:
    commands:
      - echo $CODEBUILD_BATCH_BUILD_IDENTIFIER
      - aws codebuild batch-get-build-batches --region us-east-1 --ids $CODEBUILD_BATCH_BUILD_IDENTIFIER
